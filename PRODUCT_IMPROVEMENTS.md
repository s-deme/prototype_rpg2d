# 製品化改善履歴

既存の短編RPGを「初見で迷わず、失敗しても戻れ、好みに合わせて最後まで遊べる」状態へ近づけるために行った、初期10ラウンドとその後の拡張・安定化の履歴です。現在の操作、動作環境、ビルド手順は [README.md](README.md) と [BUILDING.md](BUILDING.md) を正本とします。

## 1. 継続プレイ

課題: 終了すると進行が失われ、タイトルにも再開導線がない。

実装: バージョン付きセーブデータ、タイトルの「つづきから」、進行時・戦闘前・一定時間ごとの自動保存、ポーズからの手動保存を追加。

## 2. 安全な復帰

課題: 全滅後に最初からやり直すしかなく、開始し直すと既存進行を即座に失う。

実装: 全滅画面の直前セーブ再開、章ごとの安全座標フォールバック、新規ゲーム開始前の上書き確認を追加。

## 3. 入力の快適性

課題: 移動がキー単発入力のみで、長距離移動に連打が必要。

実装: 初回ディレイ付きキーリピート、WASD／矢印の統一、状態遷移時の入力整理を実施。

## 4. 探索導線

課題: 地名だけでは目的NPCの位置や残り距離が分かりにくい。

実装: 現在目的への方向矢印、マンハッタン距離、目的地マーカー、Qキーの詳細クエスト表示を追加。

## 5. インタラクションの発見性

課題: NPCに向いて決定する操作が画面から読み取れない。

実装: 隣接NPCの名前と「話す」プロンプト、近くのNPCのクリック会話、常時表示の主要ショートカットを追加。

## 6. 会話DX

課題: テキストが一括表示され、テンポを好みに合わせられない。

実装: タイプライター表示、決定による全文即時表示、3段階の文字速度、会話ボックスのクリック送りを追加。

## 7. 戦闘の意思決定

課題: 攻撃・魔法・回復・逃走だけでは選択の幅と敵情報が不足。

実装: ダメージ半減＋MP回復の「まもる」、HP・攻撃・逃走可否を示す「しらべる」、観察後の常時ステータス表示を追加。

## 8. バランスとアクセシビリティ

課題: プレイヤーの得意不得意に合わせる設定がない。

実装: ストーリー／スタンダード／チャレンジの3難易度で敵火力と遭遇率を調整。音量、高コントラスト、演出軽減も永続設定化。

## 9. ポインティングデバイス対応

課題: キーボード操作しか案内されず、メニュー操作の選択肢が少ない。

実装: 障害物を避けるクリック移動（幅優先探索）と、タイトル、設定、ポーズ、会話、NPC、戦闘、ゲームオーバー画面のマウス操作を追加。解像度スケールとレターボックスを考慮した座標変換にも対応。

## 10. 進捗と完了体験

課題: プレイ中・クリア時に成果を振り返れない。

実装: 歩数、戦闘勝利数、欠片、プレイ時間を記録。ポーズに「旅人」「勇者」の進捗、エンディングに最終統計を表示。READMEの操作・機能説明も更新。

## 検証チェックリスト

- 画面比率が異なる場合も960×540の論理解像度を維持する。
- セーブ値は範囲制限し、不正座標は章チェックポイントへ戻す。
- ボス戦前を含め、敗北して詰む進行状態を保存しない。
- MP不足、回復不要、アイテム不足、ボス逃走不可をメッセージで説明する。
- キーボードだけでもマウスだけでも主要フローを進められる。
- `git diff --check` とC#コンパイラの構文解析で、空白エラーと構文エラーがないことを確認する。

## 製品化拡張（残件の実装）

前回の改善後に洗い出した製品機能も実装しました。

- 入力: キー割り当て画面、左スティック／D-PadとA/B/X/Yのゲームパッド操作、フォーカス喪失時の自動ポーズ。
- セーブ: 3スロット選択、日時・章・プレイ時間プレビュー、保存時の自動バックアップ、破損時のバックアップ読み込み、旧v2セーブの移行。
- 設定: BGM／SE別音量、文字サイズ、ゆったり探索、フルスクリーン、3解像度、設定初期化。英語UI切り替えは部分翻訳を避けるため、日本語版に一本化。
- コンテンツ: 3つの宝箱、月白の茶葉から帽子屋のお守りへつながるサブクエスト、追加の通常敵、女王の弱体化技と影のMP吸収技。
- 会話と完了: 会話ログ、Shift早送り、Tabスキップ、初回チュートリアル、クリア記録、NEW GAME+。
- 配布と保守: Windowsビルド用エディタコマンド、EditModeテスト、Unity CI、クレジット、プライバシーポリシー、既知制限の文書。

## 製品安定化（P0 / P1の解消）

- セーブ復旧: 軽量な整合性チェックで壊れた通常データを「空」と誤認せず、スロット画面からバックアップを明示的に復元できるようにした。スロット削除と、現在の章を正しく示すプレビューも追加。
- 中断と再開: フォーカス喪失時は探索・会話・戦闘の復帰先を保持する。導入とチュートリアルの途中セーブも、ロード時に対応する会話から再開する。会話ログはセーブされ、新規ゲームではリセットされる。
- 操作と表示: すべてのメニューにマウスで戻る導線を用意し、宝箱・クエストもマウスで完結できるようにした。D-Pad軸を明示し、予約済みキーとの再割り当て衝突を防止。設定初期化と表示変更には確認／自動復帰を追加した。
- 可読性: 明るいパネルとエンディングで読めなかった見出し・本文を暗い前景色へ分離し、ワールド上のラベルには暗い背景を追加。タイトル下部の重なりも解消した。
- 配布: Windows用アプリアイコン、ゲーム内のクレジット・サポート画面、ビルド成果物へのDocumentation自動同梱、固定化したInput ManagerとPackage lockを追加した。
- テスト: セーブバックアップ復元、導入再開、フォーカス中断復帰、予約キーの拒否をEditModeテストに追加した。

## 再監査での追加修正

- スロット管理: 現在選択中のセーブを削除しても、別スロットに復元可能なデータがあれば自動でそちらを選択する。起動時にも空の選択スロットを安全に補正する。
- 設定の耐障害性: 壊れたキーコード、予約キー、重複したキー割り当てを読込時に既定の重複しない割り当てへ修復する。
- CI: EditModeのテスト結果を失敗時もアーティファクトとして保存し、GitHub Checksへ連携する。Windows配布物の作成はテスト成功後に限定する。
- 配布前検証: `Test-ReleasePreflight.ps1` でビルド前に必須文書、アイコン、バージョン連携、UPMロック、CI設定、テストの存在を検査できるようにした。
